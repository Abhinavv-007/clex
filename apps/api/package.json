{
  "name": "@clex/api",
  "private": true,
  "scripts": {
    "dev": "wrangler dev src/index.ts --ip 0.0.0.0 --port 8788",
    "build": "wrangler deploy --dry-run --outdir dist",
    "deploy": "wrangler deploy src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240529.0",
    "typescript": "^5.4.5",
    "wrangler": "^3.60.3"
  }
}
