{
  "name": "clex-all-in-one",
  "private": true,
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "svelte-preprocess",
      "workerd",
      "sharp"
    ]
  },
  "scripts": {
    "dev": "pnpm -r --parallel --stream --filter @clex/signaling --filter @clex/web dev",
    "dev:web": "pnpm --filter @clex/web dev",
    "dev:signal": "pnpm --filter @clex/signaling dev",
    "build": "pnpm --filter signaling build && pnpm --filter web build",
    "preview": "pnpm --filter web preview",
    "typecheck": "pnpm -r typecheck",
    "test": "vitest run"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  },
  "devDependencies": {
    "vitest": "^1.6.1"
  }
}
